# Clean-Sort
This code simplifies indexing a book, it takes a text file or multiple text files that are in a specific folder and d outputs words of a file whittled down to their base words without common less meaningful words it then counts the # of times each word occurs and outputs an alphabetically sorted text file in a different folder with occurence #s.
